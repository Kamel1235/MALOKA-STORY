[build]
  publish = "dist"
  command = "npm run build"
  ignore = "netlify/functions/"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "18"

# تعطيل Netlify Functions لأننا نستخدم نظام localStorage
[functions]
  directory = "disabled"
